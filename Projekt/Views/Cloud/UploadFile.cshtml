
@{
    ViewBag.Title = "UploadFile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("UploadFile", "Cloud", FormMethod.Post, new { encType = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="container">
        <div class="form-group">
            <br />
            <label> Upload files to Azure!</label>
            <input type="file" name="uploadFileName" value="Browse" class="btn-primary" />
            <br />
            <button type="submit" id="buttonSubmit" class="btn btn-success">Upload</button>
        </div>
    </div>
}

